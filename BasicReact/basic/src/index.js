import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));//Dom is a tree structure
root.render(
 
    <App />
 
);


//Strict mode is a property of React
//React.Strict mode is not neccesary